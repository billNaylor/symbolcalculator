package org.mechdancer.symbol.core

// {variable, power factor, exponential factor, logarithmic factor} = {factor} ⊆ {product} ⊆ {function}
// {变量，幂因子，指数因子，对数因子} = {因子} ⊆ {积} ⊆ {函数}

/**
 *Function expression
 *
 * 函数表达式
 */
interface FunctionExpression : Expression
